#ifndef UTILITY
#define UTILITY

// The utility class will offer features like showing FPS, changing level, and whatever else that fits in that category
class Utility
{
private:

public:
	void loadLevel(int levelID);
	void showFPS();
};
#endif